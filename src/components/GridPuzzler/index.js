import {
  Component,
  h,
} from 'preact';

import './style.scss';

const GridPuzzler = (props) => (
  <div>
    <h1 style={{ color: props.color }}>Hello, World!</h1>
  </div>
);

export default GridPuzzler;
